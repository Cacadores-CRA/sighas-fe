export type SubjectResponse = {
  id: string;
  code: string;
  title: string;
  shortTitle: string;
  createdAt: string;
};

export type SubjectPayload = {
  code: string;
  title: string;
  shortTitle: string;
};
